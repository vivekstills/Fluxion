// Placeholder for duel service
// TODO: Implement thin wrappers for calling the Firebase Functions

export const placeMarketOrder = async (duelId: string, side: 'BUY' | 'SELL', qty: number) => {
    // call placeOrder function
};

export const placeLimitOrder = async (duelId: string, side: 'BUY' | 'SELL', qty: number, limitPrice: number) => {
    // call placeOrder function
};
